apiVersion: v1
kind: ServiceAccount
metadata:
  name: auth-gateway
  labels:
    app.kubernetes.io/name: auth-gateway
    app.kubernetes.io/part-of: threshold

imagePullSecrets:
  - name: harbor-threshold-puller

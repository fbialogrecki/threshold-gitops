apiVersion: v1
kind: Service
metadata:
  name: auth-gateway
  labels:
    app.kubernetes.io/name: auth-gateway
    app.kubernetes.io/part-of: threshold
spec:
  selector:
    app.kubernetes.io/name: auth-gateway
  ports:
    - name: http
      port: 80
      targetPort: http

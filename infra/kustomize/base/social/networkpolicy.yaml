apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: social-ingress-from-web
  labels:
    app.kubernetes.io/name: social
    app.kubernetes.io/part-of: threshold
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: social
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: web
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: users
      ports:
        - protocol: TCP
          port: 8000

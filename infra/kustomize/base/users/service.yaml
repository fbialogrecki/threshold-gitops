apiVersion: v1
kind: Service
metadata:
  name: users
  labels:
    app.kubernetes.io/name: users
    app.kubernetes.io/part-of: threshold
spec:
  selector:
    app.kubernetes.io/name: users
  ports:
    - name: http
      port: 80
      targetPort: http

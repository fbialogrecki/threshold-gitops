apiVersion: v1
kind: Service
metadata:
  name: authentik
  namespace: internal
  labels:
    app.kubernetes.io/name: authentik-internal-alias
    app.kubernetes.io/part-of: threshold
spec:
  type: ExternalName
  externalName: authentik-server.authentik.svc.cluster.local
  ports:
    - name: http
      port: 80
      targetPort: 80

apiVersion: v1
kind: Service
metadata:
  name: social
  labels:
    app.kubernetes.io/name: social
    app.kubernetes.io/part-of: threshold
spec:
  selector:
    app.kubernetes.io/name: social
  ports:
    - name: http
      port: 80
      targetPort: http

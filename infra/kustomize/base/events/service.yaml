apiVersion: v1
kind: Service
metadata:
  name: events
  labels:
    app.kubernetes.io/name: events
    app.kubernetes.io/part-of: threshold
spec:
  selector:
    app.kubernetes.io/name: events
  ports:
    - name: http
      port: 80
      targetPort: http
